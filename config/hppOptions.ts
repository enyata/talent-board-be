import { Options } from "hpp";

const hppOptions: Options = {
  whitelist: [
    "sort",
    "fields",
    "filter",
    "location",
    "role",
    "experience",
    "skills",
    "availability",
  ],
};

export default hppOptions;
